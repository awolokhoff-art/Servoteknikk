#include "Elevator.h"

void setup() {
  // Required by Arduino — runs once
  Serial.begin(115200);
  Serial.println(F("Elevator.h compilation test"));
}

void loop() {
  // Required by Arduino — runs repeatedly
}
